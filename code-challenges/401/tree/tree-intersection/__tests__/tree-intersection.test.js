'use strict';

describe('Binary Tree Intersection', () => {
  it('returns null for invalid input', () => {});

  it('returns empty set if no common values', () => {});

  it('returns set of common values', () => {});
});
